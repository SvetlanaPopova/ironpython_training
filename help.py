def get_list(self, element=None, w_ch_list=True):
	wd=self....
        a = []
        if element is None:
                element = wd 
	element.click()?
	if len(el.els) > 0:
		for el in el.els:
			type = type
			if el.type is a folder:
				id = id
				name = name
				sub_elements = self.get_list(el, w_ch_list)
				a.append(Folder(id=id, name = name, sub_elements = sub_elements)
			else:
				if w_ch_list:
					id = id
					name = name
					a.append(Checklist(id=id, name = name)
        return a
